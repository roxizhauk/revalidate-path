{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "rm -rf .next && next build && next start",
    "update": "npx npm-check-updates -u && pnpm install"
  },
  "dependencies": {
    "next": "14.0.2-canary.16",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.8.10",
    "@types/react": "^18.2.36",
    "typescript": "^5.2.2"
  }
}
