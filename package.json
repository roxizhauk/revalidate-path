{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "rm -rf .next && next build && next start",
    "update": "npx npm-check-updates -u && npm i"
  },
  "dependencies": {
    "next": "^13.5.6-canary.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.8.6",
    "@types/react": "^18.2.28",
    "typescript": "^5.2.2"
  }
}
